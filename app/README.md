# Hootnshoot application

Monorepo source for the **Hootnshoot** social scheduling application (forked from upstream OSS).

- Root repository docs: see `/README.md` at the repo root for deployment and AWS setup.
- Local development: use the **Makefile** at the repo root (`make env`, `make infra-up`, `make dev-backend`).

Upstream licensing remains AGPL-3.0 unless your legal team directs otherwise.
